import { backgrounds } from './backgrounds.js';
import { characters } from './characters.js';

export const config = {
    backgrounds,
    characters,
    defaultBackground: 'classroom',
    defaultCharacterExpression: 'neutral',
    positions: {
        left: '20%',
        center: '50%',
        right: '80%'
    }
};